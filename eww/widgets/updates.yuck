(defpoll current_updates :interval "1h" "scripts/eww_updates_poll.sh")

(defwidget updates []
    (box
        :class "updates"
        :halign "end"
        (label :text "󰊠  ${current_updates}")
    )
)