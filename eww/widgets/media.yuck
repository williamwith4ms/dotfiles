(defpoll current_media :interval "1s" "scripts/get_media.sh" )

(defwidget media []
  (box :class "media"
    (label :class "media-label"
           :text {current_media != "" ? "${current_media}" : "Silence"}
           :limit-width 30
           :show-truncated true)))