(deflisten current_media "scripts/eww_media_listener.sh" )

(defwidget media []
  (box :class "media"
  :visible {current_media != ""}
    (label :class "media-label"
      :text "${current_media}"
      :limit-width 30
    :show-truncated true)))