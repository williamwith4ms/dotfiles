(deflisten current_volume "scripts/eww_vol_listener.sh")

(defwidget volume []
  (eventbox :onscroll "scripts/eww_volume_control.sh {}"
  (button
      :class "volume"
      :halign "end"
      :onclick "pavucontrol &"
      (label :text "${current_volume}")
    )
  )
)